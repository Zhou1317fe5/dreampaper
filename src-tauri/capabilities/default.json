{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions. core:window:allow-start-dragging 必须显式列出：它不在 core:default 里（core:window:default 只给 28 条，不含它）。titleBarStyle: Overlay 藏掉了原生标题栏，拖窗全靠前端标 data-tauri-drag-region + drag.js 调 plugin:window|start_dragging —— 少这一条权限，IPC 会被 ACL 拒掉，表现就是「整扇窗拖不动」，而且前端看不到任何报错。",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging", "dialog:allow-open", "dialog:allow-save"]
}
