# 更新日志

版本号遵循 `主版本.次版本.修订号`。安装包由 `v*` tag 触发 CI 构建产出，见 [Releases](https://github.com/dream-rec/dreampaper/releases)。

## v0.1.2

新增历史数据管理与 design 分析日志，其余为 PR #2 引入功能的修复与流畅度优化。

### 新增

- **历史数据管理页**（来自 [@Zhou1317fe5](https://github.com/Zhou1317fe5) 的 [PR #2](https://github.com/dream-rec/dreampaper/pull/2)）。列出全部历史任务，可按状态筛选，支持预览、重跑与删除。
- **多任务并行提交**（同 PR #2）。提交新任务不再需要等上一个跑完。
- **Design 分析日志**。进度栏下方按 step 逐栏展示 design model 的返回原文：科研图为「分析 template 结构」「映射内容并生成制图方案」，幻灯片为「分析 template」「规划整套大纲」以及每一页的「规划第 N 页内容」。设置页开启流式则边生成边显示，未开启则整段返回后一次显示。卡片默认折叠，点标题栏展开；当前 step 高亮；日志随任务存库，重开历史任务仍能看到当时的分析结果。

### 修复

- **历史页切换筛选条件与首次进入卡顿**。三处叠加：`.hc-image` 用 `aspect-ratio` 定高，卡片高度依赖 `1fr` 轨道解析结果，网格一变 WebKit 就把所有卡片的固有尺寸重算一遍；结果图按生成分辨率存盘（5504x3072、解码后约 68MB 是常态），却被直接画进约 400px 的卡片里；筛选原先把不匹配的卡片从树上摘掉，`<img>` 随之销毁，切回来要重新取图重新解码。改为固定像素高度、`?w=` 缩略图（7.6MB/16.9MP → 130KB/0.36MP，冷启 631ms、命中 228µs，在 webview 调用线程之外生成）、以及只切换可见性。
- **重跑不恢复历史输入**。`rerunJob` 少了 `openJob` 里的回填表单那一步，跳转过去后面板显示的和刚提交的任务对不上。
- **筛选栏高度随命中条目数变化**。面板被拉满视口高度，grid 默认的 `align-content: stretch` 把富余空间摊给了每个 auto 行。
- **历史卡片上的重跑、删除按钮过小**，且只在悬停时出现。改为常驻的 32px 带边框按钮。
- **切页动画把滚动容器整体重栅格化**，并因提升为层叠上下文而盖在左侧导航的悬浮提示之上。去掉 `scale()`，导航补 z-index。

### 其他

- 删除结果栏顶部的「进行中任务」列表。它显示的是任务标题加当前 step，与下方进度栏重复；同时去掉了它附带的第二个 1.8s 轮询——shell 本来就在轮询当前任务，运行期间的 IPC 请求量减半。并行提交能力保留在后端，界面不再单独列出队列。
- 开发档开启图像栈优化：未优化时缩放耗时约 19s，优化后约 0.6s，否则 `tauri dev` 下看起来像缓存坏了。

## v0.1.1

以修 Windows 端的问题为主，macOS 端行为不变。

### 修复

- **Windows 模版库、导入弹窗的图片预览空白**。自定义协议的 URL 形式两个平台不一样：macOS 是 `<scheme>://localhost/<path>`，而 Windows 的 WebView2 注册不了自定义协议，只能走 `http://<scheme>.localhost/<path>`。原先只按 macOS 的形式拼 URL，Windows 上自然取不到图。
- **Windows 启动时多弹一个终端窗口，且关掉它主窗口跟着退出**。发布构建缺 `windows_subsystem = "windows"`，控制台成了进程的父窗口。
- **Windows 安装版窗口不是圆角**。改为在 Win11 上通过 DWM 显式请求圆角。Win10 没有这个系统能力，保持直角，功能不受影响。
- **模版库导入弹窗里的 PaperBench 下载链接点击无反应**。Tauri 的 webview 没有 `window.open` 处理器，`<a target="_blank">` 既不跳转也不报错，URL 必须交给系统浏览器打开。
- **结果图预览点击无反应**（未报告，本次一并修）。与上一条同一个根因，改为调用系统默认看图程序打开。
- **Windows 窗口顶部约 48px 空白**（未报告，本次一并修）。`titleBarStyle: "Overlay"` 只在 macOS 生效，Windows 保留原生标题栏，为红绿灯按钮预留的位置就成了死区。

### 其他

- 项目改用 [PolyForm Noncommercial License 1.0.0](LICENSE)：允许非商业使用，禁止商业使用。
- 更换 README 里的 slide 示例截图。
- 清理仓库内约 900 行中文代码注释，注释统一改用英文。界面文案、错误提示、测试断言消息与发布说明仍为中文。

## v0.1.0

首个公开版本。
