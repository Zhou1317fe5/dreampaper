Validate that the paper figure design response is strict JSON, contains a non-empty implement_prompt, uses short visible labels, respects the selected template style, and does not request direct copying or background editing of the template image.

